version: 2
jobs:
  build:
    docker:
      - image: node:latest
    working_directory: ./
    environment:
        REPO: https://github.com/enkidevs/prototype_challenge
    steps:
      - checkout

      - run:
          name: Running tests
          command: npm test
